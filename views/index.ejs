<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EventOra</title>

    <!-- Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- css -->
    <link rel="stylesheet" href="style/main.css">
    <link rel="stylesheet" href="style/indexPage.css">
    <link rel="stylesheet" href="style/footer.css">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>

<body>
    <%- include('includes/header.ejs', {log: logged}) %>

        <section class="jumbo_section">
            <div class="container">
                <div class="row">
                    <div class="col-6 ">
                        <div class="row h-100 align-items-center">
                            <div>
                                <h1 class="jumbo_title">
                                    Il tuo <b><span id="event">evento</span></b> comincia da qui
                                </h1>
                                <div class="jumbo_paragraph">Tutti i servizi per gli eventi in una sola piattaforma.
                                </div>
                                <div class="jumbo_paragraph">Organizza qialsiasi evento aziendale o personale gestendo
                                    in modo innovattvo ogni
                                    aspetto attraverso la nostra tecnologia</div>
                            </div>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="box_img_jumbo">
                            <img class="img-fluid" src="imgs/event-bg1.jpg" alt="">
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <h3 class="my-5 text-center"> Scopri gli eventi organizzati dai nostri utenti</h3>
        <!-- all events box -->
        <div class="container mt-3">

            <div class="row justify-content-center">
                <div class="horizontal_box ">
                    <div class=" h-100 d-flex  align-items-center">
                        <% if(eventsList){ %>
                            <% for (let i=0; i< eventsList.length; i++) {%>
                                <%- include('includes/card.ejs', { card: eventsList[i]}) %>
                                    <% } %>
                                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- user events box -->
        <% if(locals.logged){ %>
            <section class="userEvents my-5 pb-5">
                <div class="container">

                    <h4 style="color: white;" class="pt-5 text-center"> <b>Ciao <%= locals.userName %>, ecco i tuoi
                                eventi:</b></h4>

                    <div class="row justify-content-center align-items-center">
                        <% for (let i=0; i< userEvents.length; i++) {%>
                            <div class="col">
                                <%- include('includes/cardUser.ejs', { card: userEvents[i]}) %>
                            </div>
                            <% } %>

                    </div>
                </div>
            </section>
            <% } %>


                <%- include('includes/footer.ejs') %>
</body>

<script src="JS/main.js"></script>
<script src="../db/database.js"></script>

</html>